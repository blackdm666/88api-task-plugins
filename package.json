{
  "name": "88api-task-plugins",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build-marketplace.mjs --out .publish",
    "prepare:host": "node scripts/prepare-host-tests.mjs"
  }
}
